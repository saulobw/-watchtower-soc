# ============================================================
# WATCHTOWER SOC PLATFORM v3.0
# Archivo: wazuh.indexer.yml
# Descripción: Configuración de OpenSearch (Wazuh Indexer)
# ============================================================

# Nombre del nodo en el cluster
# En producción habría múltiples nodos para alta disponibilidad
# En nuestro lab usamos 1 nodo (suficiente para el proyecto)
network.host: 0.0.0.0
node.name: wazuh.indexer

# Nombre del cluster - todos los nodos deben tener el mismo
cluster.name: wazuh-cluster

# Lista de nodos iniciales para formar el cluster
# Como solo tenemos 1 nodo, se apunta a sí mismo
cluster.initial_master_nodes:
  - wazuh.indexer

# Directorio donde se guardan los datos (alertas, índices)
path.data: /var/lib/wazuh-indexer

# Directorio de logs del propio Indexer
path.logs: /var/log/wazuh-indexer

# ──────────────────────────────────────────────
# CONFIGURACIÓN DE SEGURIDAD TLS
# Habilita HTTPS para que nadie pueda interceptar
# el tráfico entre el Manager y el Indexer
# ──────────────────────────────────────────────
plugins.security.ssl.http.enabled: true
plugins.security.ssl.http.pemcert_filepath: /usr/share/wazuh-indexer/certs/wazuh.indexer.pem
plugins.security.ssl.http.pemkey_filepath: /usr/share/wazuh-indexer/certs/wazuh.indexer.key
plugins.security.ssl.http.pemtrustedcas_filepath: /usr/share/wazuh-indexer/certs/root-ca.pem

plugins.security.ssl.transport.pemcert_filepath: /usr/share/wazuh-indexer/certs/wazuh.indexer.pem
plugins.security.ssl.transport.pemkey_filepath: /usr/share/wazuh-indexer/certs/wazuh.indexer.key
plugins.security.ssl.transport.pemtrustedcas_filepath: /usr/share/wazuh-indexer/certs/root-ca.pem
plugins.security.ssl.transport.enforce_hostname_verification: false

# Habilita el plugin de seguridad de OpenSearch
plugins.security.enable_snapshot_restore_privilege: true
plugins.security.check_snapshot_restore_write_privileges: true
plugins.security.restapi.roles_enabled:
  - "all_access"
  - "security_rest_api_access"

# Nodos con permisos de administración
plugins.security.nodes_dn:
  - "CN=wazuh.indexer,OU=Wazuh,O=Wazuh,L=California,C=US"

plugins.security.authcz.admin_dn:
  - "CN=admin,OU=Wazuh,O=Wazuh,L=California,C=US"
